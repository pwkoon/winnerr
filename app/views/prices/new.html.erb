<%= simple_form_for [@listing, @price] do |f| %>
  <%= f.input :tier, placeholder: 'Basic' %>
  <%= f.input :amount, placeholder: '$20' %>
  <%= f.input :description, placeholder: 'One Revision + 2 working days' %>
  <%= f.submit class: 'btn btn-flat' %>
<% end %>
